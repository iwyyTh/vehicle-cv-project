# Changelog

All notable changes to this project will be documented in this file.
Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)

## [0.1.0] - 2024-XX-XX

### Added

- `DataLoader` class with `scan()` and `summary()` methods
- `load_image_as_array()` utility for normalized image loading
- `print_image_info()` utility for image diagnostics
- `requirements.txt` with pinned dependencies
- `.gitignore` for Python/Jupyter/OS artifacts
- README with Installation and Project Structure sections

### Notes

- Week 1 foundation: Python OOP, NumPy, Matplotlib, File I/O, Git workflow
